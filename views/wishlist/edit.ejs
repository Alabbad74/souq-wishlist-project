<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Wishlist Item</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <%- include('../partials/head') %>
</head>
<body>
    <!-- LESSON 1: Basic Container Structure -->
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h2 class="mb-4">Edit Wishlist Item</h2>
                
                <!-- LESSON 2: The Edit Form - This is where the magic happens! -->
                <form action="/wishlist/<%= item._id %>?_method=PUT" method="POST">
                    <!-- 
                    🎓 TEACHING POINT: 
                    - action="/wishlist/<%= item._id %>?_method=PUT" = Where to send the data
                    - method="POST" = How to send it (HTML only supports GET/POST)
                    - ?_method=PUT = Special trick to make it a PUT request
                    -->
                    
                    <!-- LESSON 3: Form Fields Will Go Here -->
                    
                    <!-- 📝 TITLE FIELD -->
                    <div class="mb-3">
                        <label for="title" class="form-label">Item Name</label>
                        <input type="text" name="title" id="title" class="form-control" 
                               value="<%= item.title %>" required>
                        <!-- 
                        🎓 TEACHING POINT: 
                        - value="<%= item.title %>" = Pre-fills with current value
                        - name="title" = This will be sent as req.body.title
                        - required = User must fill this field
                        -->
                    </div>

                    <!-- 📝 DESCRIPTION FIELD -->
                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <textarea name="description" id="description" class="form-control" rows="3"><%= item.description %></textarea>
                        <!-- 
                        🎓 TEACHING POINT: 
                        - For textarea, content goes BETWEEN the tags, not in value attribute
                        - <textarea>CONTENT HERE</textarea>
                        -->
                    </div>
                    
                    <!-- LESSON 4: More Fields Will Go Here -->
                    
                    <!-- 💰 PRICE FIELD -->
                    <div class="mb-3">
                        <label for="price" class="form-label">Target Price (BHD)</label>
                        <input type="number" name="price" id="price" class="form-control" 
                               step="0.01" value="<%= item.price %>">
                        <!-- 
                        🎓 TEACHING POINT: 
                        - type="number" = Only allows numbers
                        - step="0.01" = Allows decimal places (like 19.99)
                        -->
                    </div>

                    <!-- 🖼️ IMAGE URL FIELD -->
                    <div class="mb-3">
                        <label for="imageUrl" class="form-label">Image URL (optional)</label>
                        <input type="url" name="imageUrl" id="imageUrl" class="form-control" 
                               value="<%= item.imageUrl || '' %>">
                        <!-- 
                        🎓 TEACHING POINT: 
                        - type="url" = Validates URL format
                        - item.imageUrl || '' = If imageUrl exists, use it, otherwise use empty string
                        -->
                    </div>

                    <!-- 💬 COMMENTS/NOTES FIELD -->
                    <div class="mb-3">
                        <label for="comments" class="form-label">Comments/Notes</label>
                        <textarea name="comments" id="comments" class="form-control" rows="3" 
                                  placeholder="Add your personal notes about this item..."><%= item.comments || '' %></textarea>
                    </div>
                    
                    <!-- LESSON 5: Buttons Will Go Here -->
                    
                    <!-- 🔘 ACTION BUTTONS -->
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success">Update Item</button>
                        <a href="/wishlist" class="btn btn-secondary">Cancel</a>
                    </div>
                    <!-- 
                    🎓 TEACHING POINT: 
                    - type="submit" = This button submits the form
                    - <a href="/wishlist"> = Cancel button goes back to wishlist
                    - d-flex gap-2 = Bootstrap classes for spacing
                    -->
                    
                </form>
                
            </div>
        </div>
    </div>
</body>
</html>